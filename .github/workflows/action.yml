---
# Python yaml load test
name: PythonTest
on:
  push:
    branches:
      - test

jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          
      - name: Inline python step
        id: setmatrix
        run: |
          import yaml
          import json
          
          with open('servers.yaml') as fh:
            read_data = yaml.load(fh,Loader=yaml.FullLoader)
          vm_list=[]
          for i in read_data:
            vm_list.append(i["name"])
          json_list = json.dumps(vm_list)
          print(json_list)
          print(f"::set-output name=matrix::{json_list}")
          
        shell: python
    outputs:
        matrix: '${{ steps.setmatrix.outputs.matrix }}'
  
  job2:
    needs: job1
    runs-on: ubuntu-latest
#     strategy:
#        matrix:
#          os-versions: ${{ fromJSON(needs.job1.outputs.matrix) }}
    steps:
     - run: echo ${{ needs.job1.outputs.matrix }}
